'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

/* eslint global-require:0 */
/* eslint import/no-dynamic-require:0 */
var fs = require('fs');
var path = require('path');
var codeblockNode = require('./codeblockNode');

var moduleDir = path.join(__dirname, 'nodes');
var files = fs.readdirSync(moduleDir);
var modelNodes = {};
files.forEach(function (node) {
	var nodeName = node.replace('.js', '');
	nodeName = nodeName.charAt(0).toUpperCase() + nodeName.substring(1);
	modelNodes['Model' + nodeName] = require(path.join(moduleDir, node));
});

exports = module.exports = _extends({}, modelNodes, {
	'Codeblock': codeblockNode
});